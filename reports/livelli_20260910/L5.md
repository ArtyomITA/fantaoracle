# Livello 5 — stagione in corso. Stato reale, 10 settembre 2026

Sessione di sola lettura su `fantabot`. Nessun file del
progetto è stato creato, modificato, rinominato o cancellato. Nessun server
avviato, nessuna rigenerazione, nessun push.

Etichette usate: **RIPRODOTTO** = comando eseguito ed esito visto in questa
sessione; **OSSERVATO NEL CODICE** = letto, non eseguito; **IPOTESI** =
deduzione dichiarata come tale.

Comandi eseguiti in questa sessione (tutti brevi, `OMP_NUM_THREADS=2`):

1. `PYTHONIOENCODING=utf-8 OMP_NUM_THREADS=2 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest tests/test_non_anticipazione.py tests/test_l2_stato_origine.py -q` → `14 passed in 5.82s`;
2. `PYTHONIOENCODING=utf-8 OMP_NUM_THREADS=2 python tests/smoke_season.py` → gira, stampa la classifica H2H su voti sintetici;
3. `python scripts/f11_refresh_all.py --elenca` → elenca gli otto passi della catena;
4. letture del pack `data/packs/pack_2026-27.pkl`, di `data/copilot/eleggibilita_2026-27.json`, dei CSV di mercato e di `data/processed/l2_partite.parquet`.

`python scripts/f11_refresh_all.py --prova` **non** è stato eseguito: pur
chiamandosi «prova», scrive `data/refresh/status.txt` e crea la cartella
`data/refresh/run_*` prima di controllare il flag (`scripts/f11_refresh_all.py`
righe 203-208 e 226). OSSERVATO NEL CODICE. Eseguirlo avrebbe violato il
vincolo di non toccare file del progetto.

## Le cinque colonne

| colonna | valore | prova |
|---|---|---|
| implementato | **parziale** | i pezzi esistono ed eseguono: `src/fantabot/season/lineup.py:28` (`pick_lineup`), `:65` (`score_giornata`), `:100` (`goals_from_points`); `src/fantabot/tabellino/partecipazione.py:320` (`stato_all_origine`); `src/fantabot/tabellino/generatore.py:319` (parametro `stato_iniziale`); `scripts/l2_genera_cubo.py:216` (flag `--solo-future`). Non esiste nessun percorso: zero righe per formazione di giornata, scambi, svincoli, riparazione |
| verificato | **parziale** | RIPRODOTTO: `14 passed in 5.82s` su `tests/test_non_anticipazione.py` + `tests/test_l2_stato_origine.py`. Coprono il contratto di non-anticipazione di `simulate_season`/`season_detail` e la definizione di `stato_all_origine`. Nessuna prova sui tre quarti mancanti di L5, perché non c'è codice da sorvegliare |
| validato | **no** | nessuna prova. Nessun confronto con criteri fissati prima su nulla di stagionale: nessuna formazione consigliata mai misurata, nessuno scambio mai valutato, nessuna riparazione mai simulata |
| collegato | **no** | `scripts/l2_genera_cubo.py:382` chiama `gen.genera(cal, rose, mp, m_part, m_ev, m_voto, n_sims=a.sims, seme=a.seme, dipendenza=struttura, fasce_sv=fasce_sv)` — `stato_iniziale` **non** viene passato. `grep -rn "stato_all_origine" src scripts tests`: compare solo in `partecipazione.py` e in `tests/test_l2_stato_origine.py`, in nessuno script. `pick_lineup` è chiamato solo da simulatori d'asta (`livello3/valutatore.py:171`, `montecarlo.py:250`, `tabellino/punteggio.py:113`, `scripts/l2_banco_confronto.py:265`) |
| operativo | **parziale** | vale per il solo punto (1). RIPRODOTTO: `data/packs/CORRENTE.json` dice `aggiornato 2026-09-10T06:25:18`, impronta `202c4299d98c…`, 594 giocatori, `limitazioni: []`; `data/refresh/status.txt` = `ok`; il pack ha `k = 3` per 594 giocatori su 594 e `votes_by_g` = `[290, 292, 291, 0, 0, …]`. Per i punti (2)(3)(4) nessuna prova, perché non esiste il comando da eseguire. E la decisione che quel giro ha servito è l'asta di stanotte, non una decisione di stagione: nessuna decisione di stagione è mai stata presa con questo codice |

## Il percorso operativo minimo, punto per punto

Contesto misurato, non supposto: RIPRODOTTO su `data/processed/l2_partite.parquet`
— stagione 2026-27, 380 partite, 30 già giocate prima del 10/9/2026, cioè tre
giornate. RIPRODOTTO su `data/raw/voti/voti_2026-27.csv` — 957 righe di voti,
319 per ciascuna delle giornate 1, 2 e 3. L'asta di stanotte cade a campionato
iniziato: L5 non è un problema di aprile, comincia domani.

### (1) Aggiornamento settimanale dei dati — **esiste**

`scripts/f8_update_market.py` scarica in sei passi indipendenti: listone
fantacalcio.it (`:46`), quotazioni fanta.soccer per giornata (`:57`), voti
della stagione corrente giornata per giornata con arresto sulla prima non
giocata (`:80-100`), Understat (`:103`), prezzi live (`:116`), probabili +
indisponibili + rigoristi (`:123`). Ogni passo salva una copia datata in
`data/snapshots/<data>` (`:39-43`). RIPRODOTTO: esistono
`data/snapshots/20260901` e `data/snapshots/20260910`.

`scripts/f11_refresh_all.py` incatena f8 → f0b_registry → f0b_match →
f0b_outputs → f1_pred → f9_market (opzionale) → f2_pack → f7_demo (opzionale),
con dipendenze dichiarate (`:68-90`) e ripristino del pack precedente se la
catena non arriva in fondo. RIPRODOTTO con `--elenca`.

Due limiti OSSERVATI NEL CODICE, entrambi rilevanti proprio per il ritmo
settimanale:

- `scripts/f4_eleggibilita.py:96` contiene `GIORNATA_CORRENTE = 4` cablato a
  mano, usato alla riga `:108` per decidere se una stima di rientro cade in una
  giornata già giocata. Alla quinta giornata il numero va cambiato a mano,
  altrimenti la logica di rientro sbaglia in silenzio. `f4` non è nella catena
  di `f11`: si lancia a parte;
- `f12_choose_objective` è stato tolto dalla catena
  (`scripts/f11_refresh_all.py:99-101`), e infatti il pack rigenerato stanotte
  è uscito senza obiettivo: `pack.b_objective` porta la nota «copiato dal pack
  del 6/9 (f12 con 100 simulazioni) nel pack rigenerato il 10/9: la catena di
  aggiornamento non include f12 e il pack usciva senza obiettivo». RIPRODOTTO
  leggendo il pack. Ogni giro settimanale ripropone lo stesso buco.

Verdetto: **esiste** come catena, **parziale** come routine settimanale senza
mani umane.

### (2) Consiglio di formazione per giornata — **manca**

Il motore c'è, il consumatore no.

`src/fantabot/season/lineup.py:28-62` sceglie modulo e undici fra i sette
moduli di riga `:11-12`, ordinando per fantamedia mobile e spingendo in fondo
chi non è disponibile (`:40-41`); tiene conto del modificatore difesa con
almeno quattro difensori (`:50-56`), scontando mezzo gradino per prudenza.
`:65-97` applica fino a tre cambi automatici e somma il modificatore a
consuntivo. `:100-103` converte i punti in gol con 66 e passo 6. Le regole
della lega ci sono tutte.

Manca chi lo chiami con una giornata vera. `grep` su tutto il progetto: i
chiamanti di `pick_lineup` sono `livello3/valutatore.py:171`,
`montecarlo.py:250`, `tabellino/punteggio.py:113`,
`scripts/l2_banco_confronto.py:265`, più `season/simulate.py:87` e
`season/detail.py:46` — tutti dentro simulazioni per valutare una rosa d'asta.
Nessuno script stampa undici nomi per la giornata che si gioca. `viz/` ha
`copilot.html`, `index.html`, `replay.html`: `replay.html` mostra le
formazioni della stagione **simulata**, non una formazione da schierare.

Gli ingressi però esistono già, ed è la parte che rende il passo piccolo.
RIPRODOTTO: `data/raw/mercato/probabili_20260910.csv` ha 473 righe con le
colonne `giornata, squadra, avversario, casa, nome, ruolo, pct_titolarita,
stato, lista, ballottaggio_con, pct_ballottaggio, player_id`; sono tutte della
giornata 4; 471 righe hanno `player_id` e **467 di quei 471 sono chiavi del
pack** — lo spazio di identità coincide, non serve un matching per nome.
`data/copilot/eleggibilita_2026-27.json` porta 54 indisponibili già risolti a
`master_id`, con tipo, testo e stima di rientro.

Un limite dei dati grezzi, OSSERVATO: `data/raw/mercato/indisponibili_20260910.csv`
ha colonne `squadra, nome, tipo, dettaglio, rientro_atteso, rientro_stima` —
**nessun identificatore**. La risoluzione a `master_id` avviene solo dentro
`scripts/f4_eleggibilita.py`. Chi vuole gli indisponibili deve passare da lì,
non dal CSV.

Verdetto: **manca**, ma è il pezzo più corto da costruire di tutto L5.

### (3) Valutazione di scambi e svincoli — **manca**

Zero righe. `grep -rn -iE "svincol|riparazion"` su `src/` e `scripts/` non
trova nessuna funzionalità: le uniche occorrenze di «scambio» sono
`src/fantabot/livello3/ricerca.py:420-486` (`scambi_locali`, miglioramento
locale di una rosa **d'asta** dentro l'ottimizzatore) e
`src/fantabot/livello3/indifferenza.py:463` (scambi fra giocatori *pianificati*
nella simulazione corrente). Sono operazioni sul piano d'asta, non transazioni
di lega.

Manca anche la regola, prima del codice. `PIANO.md:188-197` descrive il modulo
riparazione come progetto del 7/8 («da costruire su richiesta»): svincoli
forzati con rimborso 100 %, svincoli volontari fino a K slot con rimborso 50 %,
pool di riparazione, ri-scoring CatBoost sulle giornate 1-19. Sono scelte
proposte, non regole confermate dalla lega. `config/league.yaml` non dice nulla
su quanti svincoli o con che rimborso: ha solo `repair_context.extra_credits: 100`
e `repair_context.typical_leftover: "<50"` (righe 35-37).

E `config/league.yaml` non è letto da nessuno script — lo dichiara il file
stesso alle righe 9-11 e lo conferma `reports/LIVELLO0_E_1.md:175`
(«nessuno script lo legge»). RIPRODOTTO con `grep -rn "league.yaml"`: le
uniche occorrenze sono in file di documentazione.

Verdetto: **manca**, e prima del codice manca la regola.

### (4) Asta di riparazione di gennaio con 100 crediti — **manca**

`config/league.yaml:38-48` chiude con `repair_auction: false`;
`repair_context.extra_credits: 100` alle righe 35-36, con il commento «Corretto
il 6/9/2026: … i crediti aggiunti a gennaio sono 100 (non 50), come confermato
dal committente». Nessuno script legge nessuna delle due chiavi.

**Contraddizione trovata, e sta nel documento che si legge stasera.**
`ASTA_VERA.md:29` dice: «Chiudi l'asta con **pochi crediti residui**: i crediti
non spesi valgono zero (la lega aggiunge 50 crediti a gennaio per la
riparazione)». Cinquanta contro cento. RIPRODOTTO con
`grep -rn "50 crediti\|100 crediti" --include=*.md`. La guida operativa è
rimasta indietro rispetto alla correzione del 6/9. Non cambia il consiglio di
fondo (spendere quasi tutto), ma è un numero sbagliato in un documento
d'azione, ed è l'unico punto di L5 che tocca l'asta di stanotte.

Verdetto: **manca** come codice; il contesto di lega è dichiarato in un file
che nessuno legge e contraddetto in un file che si legge.

## Che cosa è cambiato rispetto al 9/9

Verificato per lettura dei file e per data di modifica (`ls --time-style=full-iso`).

**Cambiato, e riguarda l'asta:**

- pack rigenerato: `data/packs/pack_2026-27.pkl` del 2026-09-10 06:25, impronta
  `202c4299d98c…`, **594** giocatori (i rilievi di stanotte in `w1/regole.md` e
  `w1/caccia_dati.md` parlano di `db2584ea408b…` e 587: quello è il pack
  precedente, non più in linea);
- `k = 3` per tutti e 594 i giocatori (RIPRODOTTO); i voti della terza giornata
  sono nel pack: `votes_by_g` = `[290, 292, 291, 0 × 35]`. Il rilievo **D6** di
  `w1/caccia_dati.md` («pack senza i voti della 3ª giornata») risulta chiuso;
- `data/copilot/eleggibilita_2026-27.json` ricostruito e riallineato: il campo
  `pack` porta l'impronta nuova `202c4299d98c…` e 594 giocatori; 54
  indisponibili, 0 esclusi;
- `scripts/f10_copilot.py` modificato alle 06:23 e `scripts/f11_refresh_all.py`
  all'01:15 del 10/9. Effetto collaterale sulle citazioni: il report del 9/9
  cita `scripts/f10_copilot.py:342` come la riga che legge il pack; oggi la
  riga 342 è `quote = quote or dict(STATE["quotas"])` e le letture del pack
  stanno alle righe 178, 265 e 1091. La citazione è **scaduta**, non era falsa
  quando è stata scritta.

**Non cambiato, e riguarda L5:** `src/fantabot/season/lineup.py` è fermo al
4/9, `simulate.py` e `detail.py` al 6/9, `scripts/l2_genera_cubo.py` all'8/9
alle 23:34. In L5 non è stato scritto nulla dopo il report.

## Che cosa il report del 9/9 dice di falso

**1. Il pezzo mancante è indicato male.** `reports/STATO_LIVELLI_20260909.md:101-102`:
«Il generatore continua però a ciclare su tutto il calendario: simulare **solo**
il futuro è il pezzo che manca».

È vero della funzione `genera`, che cicla sul calendario che riceve
(`src/fantabot/tabellino/generatore.py:339-341`). È **falso** del percorso:
`scripts/l2_genera_cubo.py:216-219` dichiara `--solo-future` («genera solo le
giornate non ancora giocate alla data limite: è l'orizzonte vero di un'asta a
campionato iniziato») e le righe `:232-238` tagliano davvero il calendario,
con un commento che spiega perché le giornate concluse non vanno simulate.
Il flag finisce anche nel rapporto del cubo (`:405`, `"solo_future"`). E quel
file porta data 8/9 alle 23:34, cioè era su disco **prima** che il report
fosse scritto (`reports/STATO_LIVELLI_20260909.md`, modificato il 10/9 alle
00:06).

Il pezzo che manca davvero è un altro, ed è più grave perché invisibile:
`scripts/l2_genera_cubo.py:382` chiama `gen.genera(...)` **senza**
`stato_iniziale`. Quindi anche lanciando `--solo-future`, ogni giocatore
riparte da un'estrazione dalla marginale con `run = 1` — esattamente ciò che
`stato_all_origine` è stato scritto per evitare
(`src/fantabot/tabellino/partecipazione.py:323-329`: «è corretto per una
stagione interamente futura, ed è sbagliato a stagione iniziata, perché butta
via l'informazione che si ha»). Le due metà esistono, sono verificate una per
una, e non sono unite da nessuna riga di codice. Il report ha dichiarato
mancante la metà che c'era e non ha visto la giuntura assente.

**2. «implementato: no» è impreciso.** La riga 17 della tabella mette L5 a
`no` su implementato e `—` sulle altre colonne. I pezzi elencati sopra
esistono ed eseguono, e stamattina 14 prove che li sorvegliano passano. La
lettura giusta è: pezzi sì, percorso no. Metterlo a `no` secco fa sembrare L5
un foglio bianco, e nasconde che tre delle quattro cose che servono a
febbraio hanno già il motore in casa.

**3. Non falso, ma incompleto.** Il report dice «Esiste l'aggiornamento dei
dati» senza dire che ha due punti che vanno toccati a mano ogni settimana
(`GIORNATA_CORRENTE` cablato, `f12` fuori dalla catena). Vedi punto (1).

Fuori dal report ma dentro L5: la contraddizione `ASTA_VERA.md:29` (50) contro
`config/league.yaml:36` (100) descritta al punto (4).

## Una prova decisiva

**Che cosa decide.** Se il punto (1) — l'unica parte di L5 che oggi si può
chiamare operativa — regge un **secondo** giro settimanale senza mani umane,
o se il giro del 10/9 è stato un'operazione assistita.

**Quando.** Dopo la quarta giornata, cioè dal 14/9/2026, quando i voti della
g4 sono pubblicati.

**Dati necessari.** Rete verso fantacalcio.it, fanta.soccer e Understat; voti
della giornata 4 pubblicati; il pack corrente come riferimento
(`202c4299d98c…`, `k = 3`, `votes_by_g` con tre giornate piene).

**Comando.** Va spezzato, perché la catena intera supera il limite dei 15
minuti per comando (lo scaricamento di f8 e `f1_make_predictions` sono i due
tratti lunghi):

```bash
cd "fantabot"
PYTHONIOENCODING=utf-8 OMP_NUM_THREADS=2 python scripts/f11_refresh_all.py --solo f8
PYTHONIOENCODING=utf-8 OMP_NUM_THREADS=2 python scripts/f11_refresh_all.py --da f0b_registry
PYTHONIOENCODING=utf-8 OMP_NUM_THREADS=2 python -c "import sys,pickle,collections; sys.path.insert(0,'src'); p=pickle.load(open('data/packs/pack_2026-27.pkl','rb')); print(collections.Counter(v.get('k') for v in p.b_predictions.values())); print([len(v) for v in p.votes_by_g][:6]); print(bool(getattr(p,'b_objective',None)))"
```

**Durata stimata.** 20-40 minuti in tutto, dominata dallo scaricamento; il
secondo comando resta sotto i 15 minuti se f8 è già andato.

**Criterio di accettazione, scritto prima di guardare qualunque numero.**
Il punto (1) di L5 si dichiara *operativo* solo se **tutte e cinque** valgono:

1. entrambi i comandi escono con codice 0 e `data/refresh/status.txt` contiene `ok`;
2. `k == 4` per tutti i giocatori del pack (oggi è 3 per 594 su 594);
3. `[len(v) for v in pack.votes_by_g][:6]` mostra quattro giornate non vuote e la quarta con almeno 250 righe (le prime tre stanno fra 290 e 292);
4. `data/packs/CORRENTE.json` porta un'impronta diversa da `202c4299d98c…` e `limitazioni: []`;
5. `pack.b_objective` è presente **senza** che nessuno lo copi a mano.

Se cade anche solo la 5 — cioè se l'obiettivo va di nuovo incollato dal pack
precedente — il punto (1) resta **parziale** e va detto così: la catena gira,
la routine no. Nessuna soglia si sposta dopo aver visto l'esito, e un giro
riuscito non dice nulla sui punti (2), (3) e (4), che restano *manca*.

## Passi minimi

Nessuno di questi passi è stato eseguito. Il progetto non è un repository git:
il ripristino è per copia, e la copia va fatta **prima** di toccare il file.

### P1 — unire `stato_iniziale` al cubo

- **file**: `scripts/l2_genera_cubo.py` — import di `stato_all_origine` accanto agli altri, e la sola chiamata alla riga 382. Nessuna modifica sotto `src/`.
- **test**: nuovo `tests/test_l5_cubo_stato_iniziale.py`. Sostituisce `gen.genera` con una spia e verifica che, con `--solo-future`, l'argomento `stato_iniziale` arrivi non nullo e che il rapporto del cubo contenga la diagnostica di `stato_all_origine`.
- **accettazione**: con `--as-of 2026-09-11 --solo-future` il rapporto riporta `giocatori_con_stato_osservato > 0` e `giocatori_con_stato_ignoto` esplicito; **senza** `--solo-future` il cubo resta bit per bit quello di oggi a parità di seme (nessuna regressione sui percorsi L2 esistenti).
- **ripristino**: `copy scripts\l2_genera_cubo.py scripts\l2_genera_cubo.py.bak` prima; ripristino per copia inversa.

### P2 — consiglio di formazione per giornata

- **file**: nuovo `scripts/f17_formazione.py`. Legge il ledger della sessione d'asta (`data/copilot/ledger_*.json`, chiavi `season, names, my_index, budget, quotas, events`), `data/raw/mercato/probabili_<data>.csv` filtrato sulla giornata, `data/copilot/eleggibilita_<stagione>.json` per i 54 indisponibili, il pack per `b_predictions`. Chiama `pick_lineup` di `src/fantabot/season/lineup.py`. Nessun file esistente modificato.
- **test**: nuovo `tests/test_l5_formazione.py`, su rosa fittizia da 25, senza rete e senza pack.
- **accettazione**, fissata prima: (a) l'uscita ha esattamente 11 titolari, modulo compreso nei sette di `lineup.py:11-12`, quote di ruolo rispettate; (b) un titolare presente negli indisponibili scende in panchina se esiste un'alternativa di ruolo disponibile, e resta titolare — con avviso stampato — se il reparto non basta; (c) a parità di ingressi l'uscita è identica fra due esecuzioni. Nessun criterio sul punteggio: una giornata sola non valida niente, e va dichiarato.
- **ripristino**: file nuovi, si cancellano.

### P3 — togliere la giornata cablata

- **file**: `scripts/f4_eleggibilita.py:96` (`GIORNATA_CORRENTE = 4`) → opzione `--giornata`, con default dedotto dalla prima giornata non giocata di `data/raw/calendario/calendario_2026-27.csv` (colonna `giocata`).
- **test**: nuovo `tests/test_l5_giornata.py`. Con `--giornata 5`, una stima di rientro `G4` non viene più marcata «giornata già giocata»; senza `--giornata`, il default coincide con la prima riga a `giocata == 0`.
- **accettazione**: rieseguendo con `--giornata 4` l'uscita coincide con l'attuale `data/copilot/eleggibilita_2026-27.json` in ogni campo tranne `costruito_il`.
- **ripristino**: copia `.bak` prima della modifica.

### P4 — allineare il numero dei crediti di gennaio

- **file**: `ASTA_VERA.md:29` («50 crediti») contro `config/league.yaml:36` (`extra_credits: 100`). Si corregge il documento, non il modello: nessun codice legge nessuno dei due.
- **test**: nuovo `tests/test_l5_coerenza_crediti.py` — nessun `.md` del progetto (escluso `reports/_archivio/` e `data/istantanee/`) può dire «50 crediti a gennaio» mentre `league.yaml` dice 100.
- **accettazione**: il test fallisce **oggi**, il che dimostra che il difetto esiste, e passa dopo la correzione della singola riga.
- **ripristino**: una riga di markdown.

### P5 — prima della riparazione, la regola

Non è un passo di codice. `PIANO.md:188-197` è un progetto, non un
regolamento: quanti svincoli, con che rimborso, in che finestra, e se il pool
comprende i nuovi arrivi di gennaio sono decisioni del committente, non
deducibili dai dati. Passo minimo: scriverle in `config/league.yaml` sotto
`repair_context` e — questo è il punto — far leggere quel file a qualcosa. Oggi
`league.yaml` è un promemoria che nessuno script apre, e finché resta tale
qualunque numero scritto lì non ha effetto su nessuna decisione.

## Sintesi in una riga

Di L5 esistono il motore di formazione e lo stato all'origine, entrambi
verificati e nessuno dei due collegato; esiste e gira l'aggiornamento dei dati,
con due manopole da girare a mano ogni settimana; formazione di giornata,
scambi, svincoli e riparazione sono zero righe di codice, e per gli ultimi due
manca prima la regola.
