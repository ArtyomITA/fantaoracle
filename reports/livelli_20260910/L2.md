# Livello 2 — cubo TABELLINO: stato reale al 10 settembre 2026

Rilievo in sola lettura, eseguito la notte del 10 settembre 2026 mentre l'asta
vera è fissata per le 19:30. Nessun file del progetto è stato modificato,
creato, rinominato o cancellato. L'unica esecuzione è la suite pytest del cubo,
che il compito autorizza esplicitamente.

Ogni affermazione porta una marca: **RIPRODOTTO** (comando eseguito stanotte,
con esito riportato), **OSSERVATO NEL CODICE** (letto in un file, con
riferimento riga), **AFFERMATO** (un report lo dice, non è stato riprodotto),
**IPOTESI** (mia deduzione, non provata).

---

## 1. Le cinque colonne

| colonna | valore | prova |
|---|---|---|
| implementato | sì | RIPRODOTTO: 361 prove raccolte, 360 passate, 1 saltata in 125,72 s. Codice in `src/fantabot/tabellino/` (16 moduli) e `scripts/l2_*.py` (21 script). L'ultima esecuzione del pilota registrata su disco porta l'impronta dello script che oggi sta sul disco. |
| verificato | sì, con una crepa dichiarata | RIPRODOTTO: le 361 prove `tests/test_l2_*.py`. Crepa: due dei tre presidi citati dal report del 9/9 non certificano il codice oggi su disco (§3). |
| validato | no | Nessuna prova riproducibile misura il cubo dove il cubo lavora. Il verdetto del banco più recente è contro il cubo. Vedi §4. |
| collegato | no | RIPRODOTTO: nessuno dei tre pack contiene `theta` o il cubo; `grep -n "cubo\|tabellino"` su `scripts/f10_copilot.py`, `scripts/fantaoracle_app.py`, `src/fantabot/optimizer.py`, `src/fantabot/bots/bot_b.py` non produce nessuna riga. |
| operativo | no | Stessa prova. L'asta di stasera gira su `data/packs/pack_2026-27.pkl`, impronta `202c4299d98c2e08…`, 594 giocatori, dichiarato in `data/packs/CORRENTE.json`. Quel pack non porta niente del cubo. |

### Il comando eseguito, e il suo esito

```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONIOENCODING=utf-8 PYTHONPATH=src \
  OMP_NUM_THREADS=2 python -m pytest tests/test_l2_*.py -q
```

```
360 passed, 1 skipped, 1 warning in 125.72s (0:02:05)
```

Raccolte 361 prove su 20 file. Ripartizione per file, dal conteggio delle
funzioni `test_`: verdetto 27, verificatore 26, inferenza 23, generatore 20,
origine 20, calibrazione 19, esecuzione 18, configurazione 16, distribuzione 16,
appartenenza\_tempo 15, panel 15, contratto 13, esecuzione\_esclusiva 13,
ingressi\_c2 13, viste\_temporali 13, partita 11, presenze 11,
stato\_origine 11, progressivo 9, percorso\_dati 8. La differenza fra 317
funzioni e 361 prove raccolte viene dalle parametrizzazioni.

L'unico avviso è un `FutureWarning` di pandas in
`tests/test_l2_verificatore.py:334`, dovuto a una scrittura volutamente
incompatibile dentro il test stesso.

### Le prove del pack e dei consumatori

RIPRODOTTO. Le chiavi di `SeasonPack` nei tre pack (`2024-25`, `2025-26`,
`2026-27`) sono: `season`, `players`, `votes_by_g`, `quotas`, `budget`,
`b_predictions`, `a_price_list`, `voti_by_g`, `use_mod_difesa`, `b_objective`.
Nessun `theta`, nessun cubo, nessuna traccia del Livello 2.

RIPRODOTTO. `grep -rn "cubo_.*\.pkl"` trova sette consumatori del cubo, tutti
sperimentali: `scripts/a1_due_attaccanti.py:104`, `scripts/l2_coda_gol.py:79`,
`scripts/l2_genera_cubo.py:469` (che lo scrive), `scripts/l3_confronto_asta.py:280`,
`scripts/l3_pilota.py:50`, `scripts/l3_tetti.py:194`. Tutti appartengono a rami
che `RIPRESA.md` §1 dichiara sospesi (L3) o a esperimenti chiusi. Nessuno sta
sul percorso che stasera prende una decisione.

---

## 2. Che cosa è cambiato dal 9 settembre

### 2.1 Il codice del Livello 2 non è cambiato

RIPRODOTTO. `find src scripts tests data/packs -newermt "2026-09-10 00:00" -type f`
elenca dodici file: `src/fantabot/bots/bot_b.py`, `src/fantabot/optimizer.py`,
`src/fantabot/piani.py`, `src/fantabot/rettifica_indisponibili.py`,
`scripts/f10_copilot.py`, `scripts/f11_refresh_all.py`,
`scripts/f14_listino_offline.py`, `scripts/f4_eleggibilita.py`,
`scripts/fantaoracle_app.py`, `scripts/l1_presenze_per_origine.py`,
`tests/test_copilot_asta.py`, `tests/test_rettifica_indisponibili.py`, più
`data/packs/CORRENTE.json` e `data/packs/pack_2026-27.pkl`.

Nessun file di `src/fantabot/tabellino/`, nessuno `scripts/l2_*`, nessun
`tests/test_l2_*`. Le correzioni al Copilota non hanno toccato il cubo.

### 2.2 Ma due dei quattro ingressi del cubo 2026-27 sono cambiati stanotte

RIPRODOTTO. `data/l2/cubo_rapporto_2026-27.json` registra, per il panel
2026-27, quattro impronte di fonte. Ricalcolate stanotte con lo stesso
algoritmo del produttore (`scripts/l2_costruisci_panel.py:134`, sha256 troncato
a 16 caratteri):

| fonte | file | registrata nel cubo | oggi |
|---|---|---|---|
| voti | `data/raw/voti/voti_2026-27.csv` | `cafd8667fce11d00` | **`c0ccb3bf1a882976`** |
| listone | `data/processed/players_2026-27.parquet` | `bfdab5a98fc0fe44` | **`2d53a2e5ed0a10fa`** |
| partite | `data/processed/l2_partite.parquet` | `f725c558b6da934b` | `f725c558b6da934b` |
| game\_lineups | `data/raw/transfermarkt/_download/game_lineups.csv.gz` | `5e83d6fad28364aa` | `5e83d6fad28364aa` |

`data/l2/cubo_2026-27.pkl` è dell'8 settembre alle 23:15. Due delle sue quattro
fonti sono state riscritte stanotte dalla catena che ha rigenerato il pack.
L'artefatto è quindi **stale**, e non era stale il 9 settembre.

### 2.3 Il bersaglio del cubo 2026-27 è cambiato senza che nessuno se ne accorga

OSSERVATO NEL CODICE. `scripts/l2_genera_cubo.py:91` legge
`data/processed/b_predictions_{stagione}.json`.

RIPRODOTTO. `data/processed/b_predictions_2026-27.json` è stato riscritto
stanotte alle 06:20; il cubo che lo consuma è dell'8 settembre alle 23:15.

RIPRODOTTO. La stringa `b_pred` **non compare** in
`data/l2/cubo_rapporto_2026-27.json`: il rapporto del cubo non registra
l'impronta del proprio bersaglio. Nessun controllo automatico può quindi
accorgersi che l'ingresso principale è cambiato. È lo stesso tipo di difetto
che `RIPRESA_L2_L3.md` §2.6 aveva già registrato per `banco_partita`, su un
altro file.

### 2.4 Il ramo su cui vive il candidato C2 è intatto

RIPRODOTTO. Le impronte degli ingressi del 2024-25 coincidono con quelle
registrate dall'esecuzione del candidato
(`data/l2/prove/2024-25__20260909T201500__89314011c6c4/esecuzione.json`):
`voti_2024-25.csv` = `ce9581ac61006cd3`, `players_2024-25.parquet` =
`700a9b471e21780a`, `l2_partite.parquet` = `f725c558b6da934b`,
`b_predictions_2024-25.json` = `031f946f6a33eacf…`. Il candidato congelato non
è stato invalidato dalla notte.

---

## 3. Che cosa il report del 9 settembre dice, e che cosa regge

`reports/STATO_LIVELLI_20260909.md` è stato scritto alle 00:06 del 10
settembre. Riga per riga, sul Livello 2:

**«Implementato e verificato. Il pilota C2 gira.»** — regge. RIPRODOTTO
indirettamente: l'esecuzione `2024-25__20260909T234040__28e4c59b3477` porta
`impronta_script: e688e36901b3fa86…`, che è l'impronta del file
`scripts/l2_pilota_c2.py` oggi su disco (ricalcolata stanotte). Il codice
attuale ha girato davvero, sia pure in modalità prova (3 iterazioni, `sims 2`).

**«Il controfattuale temporale esercita il produttore vero e passa.»** — vero
per un produttore che non è più quello su disco. RIPRODOTTO:
`data/l2/prove/c2_controfattuale_2024-25.json` registra
`impronta_produttore: 36458d8aa0e07e76`; `scripts/l2_pilota_c2.py` oggi vale
`e688e36901b3fa86`. L'artefatto è delle 23:33:13 del 9 settembre, il file del
pilota è stato modificato alle 23:39:50, sei minuti dopo. Anche
`scripts/l2_controfattuale_c2.py` è stato modificato alle 23:33:25, dodici
secondi dopo l'artefatto che dovrebbe certificarlo. Il meccanismo esiste e ha
passato; **non certifica il codice che si eseguirebbe adesso**.

**«La verifica del candidato controlla che gli ingressi coincidano prima di
applicare `theta`.»** — vero per i dati, falso per il codice. OSSERVATO NEL
CODICE: `coincidenza_col_candidato` (`scripts/l2_verifica_c2.py:64-128`)
confronta le impronte dei file di dati, le impronte dei moduli, il bersaglio
per giocatore e l'insieme dei parametri. Non confronta `impronta_script`, che
il piano registra a fianco di `impronte_ingressi` e non dentro. RIPRODOTTO: il
candidato `89314011c6c4` è stato prodotto dallo script `6803831dafa51bef…` con
`passo_voluto: 0.2`; il codice su disco è `e688e36901b3fa86…` con
`PASSO_VOLUTO = 0.05` (`scripts/l2_pilota_c2.py:119`). La verifica delle 23:37
dichiara comunque `"stessi_ingressi": true` e
`"verdetto": "stesso candidato: ingressi identici a quelli dell'esperimento"`.
Il commento del pilota, righe 102-118, dice esplicitamente che chi rieseguisse
con questo codice non otterrebbe gli stessi numeri: il controllo automatico non
lo intercetta.

**«Differenze appaiate a molte decine di errori standard.»** — regge.
RIPRODOTTO dal log `data/l2/log_pilota_c2_v2.txt`: contro il bersaglio grezzo
`d = −0,07014`, `es = 0,00203`, cioè 34,6 errori standard; contro il bersaglio
obiettivo `−0,06756 / 0,00206`, 32,8. Contro l'osservato, che è diagnostico,
`−0,01519 / 0,00121`, 12,6.

**«Su 30 giocatori di una squadra, una stagione.»** — regge. RIPRODOTTO:
`parametri: 30`, `squadre ['Fiorentina']`, stagione 2024-25.

**«`theta` non è in nessun pack, e nessun consumatore lo legge.»** — regge,
RIPRODOTTO in §1.

### Due affermazioni sbagliate, in due documenti diversi

**Nella tabella del 9/9, riga del Livello 1: «sì, 562 prove».** 562 è la suite
intera, come `RIPRESA.md` §1 dichiara a chiare lettere («La suite: 562 passati,
1 saltato»). Stanotte ho riprodotto che 361 di quelle 562 sono prove del
Livello 2. Attribuire 562 prove al Livello 1 nella riga del Livello 1, mentre
la riga del Livello 2 porta un «sì» senza numero, sposta il merito di 361
prove dal livello che le ha al livello sopra.

**In `RIPRESA.md` §3bis, terzo limite: «il controfattuale temporale ha potenza
su tre grandezze su cinque: `bersaglio` e `squadre` non si muovono nemmeno
perturbando i ruoli anteriori».** Contraddetto dall'artefatto su disco.
RIPRODOTTO leggendo `data/l2/prove/c2_controfattuale_2024-25.json`:
`"dipendenze_senza_potenza": {}`, `"passato": true`, dieci grandezze dichiarate
e sei perturbazioni, e la perturbazione «ruoli ruotati P→D→C→A→P nella mappa
esterna» muove `bersaglio_obiettivo`, `mappa_ruoli`, `parametri` e
`squadre_scelte`. L'artefatto è delle 23:33 del 9 settembre; `RIPRESA.md` è
stato scritto alle 00:06 del 10, cioè **dopo**, e riporta lo stato precedente.
Il limite dichiarato non è più vero; il documento che fa da puntatore corrente
lo afferma ancora.

Va detto anche il rovescio: l'artefatto che smentisce il limite è stato
prodotto da uno script modificato dodici secondi più tardi, quindi neanche
questa smentita è certificata sul codice attuale. Le due cose insieme dicono la
stessa cosa: **il controfattuale va rieseguito**, e finché non lo è nessuna
delle due versioni della frase è dimostrata.

---

## 4. Che cosa manca per «validato»

Il report del 9/9 elenca quattro luoghi dove il vantaggio del cubo andrebbe
misurato: dipendenze, durata delle assenze, punteggi di rosa, decisioni
d'asta. Stato di ciascuno.

### Dipendenze

Esiste una misura descrittiva, non un verdetto. RIPRODOTTO leggendo
`data/l2/cubo_rapporto_2026-27.json`: correlazioni fra compagni sul voto puro,
osservate contro simulate — portiere-difensori 0,2919 contro 0,1695;
difensori 0,4042 contro 0,3832; centrocampisti 0,3779 contro 0,2806;
attaccanti 0,3629 contro 0,2603. Il cubo sottostima ogni correlazione, e di
molto sulla coppia portiere-difensori (−42 %).

Manca il pezzo che trasforma questo in validazione: nessun criterio di
accettazione è stato scritto prima su queste quantità.
`reports/CRITERI_L2_L3.md` §3.4 («Cubo (F6)») fissa soltanto criteri di
coerenza fisica — zero violazioni, un portiere sempre in campo, rigori
appaiati, minuti conservati, porta inviolata una volta sola. §3.7 fissa la
regola di promozione del banco su CRPS e Brier, cioè su marginali. Le
correlazioni compaiono in §3.7 come «quantità di realismo», riportate ma non
decisive. **Quindi oggi non esiste una soglia dichiarata prima che il cubo
possa superare o mancare sulle dipendenze.**

### Durata delle assenze

Non misurata, e in parte non modellata per scelta dichiarata. OSSERVATO NEL
CODICE: `src/fantabot/tabellino/partecipazione.py:339-341` — «Non deduce
infortuni. L'assenza di una riga, o un `senza_voto`, dice che il giocatore non
ha preso voto: non dice perché. Attribuire una causa medica a un'assenza
sarebbe inventare uno stato latente che i dati non contengono.»

Una durata esiste comunque, come memoria della catena: `partecipazione.py:10`
descrive «una catena con memoria», e `stato_all_origine`
(`partecipazione.py:322-345`) restituisce `(presente, run)` con `run` troncato
a `MAX_RUN`. Quindi le strisce di assenza hanno persistenza nel generatore.

RIPRODOTTO: `grep -rln "striscia\|durata_assenz\|spell"` su `scripts/` e
`tests/` trova solo tre file di prova
(`tests/test_l2_appartenenza_tempo.py`, `tests/test_l2_stato_origine.py`,
`tests/test_l2_viste_temporali.py`) e nessuno script di misura. **Non esiste
nessun artefatto che confronti la distribuzione della durata delle assenze
simulate con quella osservata.** Questa colonna è vuota, non parziale.

### Punteggi di rosa

Misurati una volta sola, su un campione che non regge un verdetto. RIPRODOTTO
leggendo `data/l2/prove/2024-25__20260909T233755__cf5c9ee9d8df/verifica_c2_2024-25.json`:
10 rose, 6 scenari, 3 semi, 2 gradi di libertà, `t_critico_95 = 4,303`, con una
nota nell'artefatto stesso che dichiara l'errore standard «esso stesso molto
incerto». I numeri: `rosa_punti_media` C1 55,286 contro C2 55,399, differenza
`+0,1125` con `es 0,00829`; `rosa_punti_sd` 10,711 contro 10,721, differenza
`+0,0092` con `es 0,0415`, cioè un intervallo che attraversa lo zero.

Va letto per quello che è: un confronto fra due varianti del cubo, non fra il
cubo e qualcos'altro. Non dice se i punteggi di rosa del cubo somiglino a
quelli veri.

### Decisioni d'asta

Il cubo è già usato come banco nel confronto d'asta
(`scripts/l3_confronto_asta.py:280`), ma quei numeri sono inutilizzabili:
AFFERMATO in `RIPRESA.md` §4.6 e `reports/STATO_LIVELLI_20260909.md`, «nessun
esperimento è stato rigiocato dopo le correzioni: i numeri L3 pubblicati
vengono dal bot difettoso». Non ho riprodotto questa parte, che è di Livello 3.

### Il verdetto che esiste, ed è contro

AFFERMATO in `RIPRESA.md` §2, con artefatti e impronte: a informazione
comparabile il cubo perde. `C1 − I1` su CRPS empirico vale `+0,097268` sul
2024-25 e `+0,050467` sul 2025-26, con intervalli two-way che escludono lo
zero su entrambe le stagioni, entrambe le misure, entrambi i punteggi.
Non riprodotto stanotte: la riproduzione costa due corse di
`scripts/l2_banco_confronto.py` con `--sims 30 --semi 8`, fuori dal budget di
quindici minuti e fuori dal mandato in sola lettura.

### Il vizio che sta sotto tutto

AFFERMATO in `RIPRESA.md` §3ter, misurato il 9 settembre: il bersaglio del cubo
viene da un CatBoost su 36 feature, due delle quali (`fvm` e `quot_fs_sett`)
non sono disponibili alla data dell'asta. Togliendole, l'errore assoluto medio
del modello passa da 5,5688 a 8,1259 presenze sul 2024-25. Il cubo eredita lo
stesso bersaglio, quindi nessuno dei due lati del confronto bersaglio/cubo è
pulito.

**Conclusione della colonna: no.** Non «inconcludente», non «quasi»: dei
quattro luoghi indicati, uno ha misure senza criterio (dipendenze), uno non ha
misure affatto (durata delle assenze), uno ha una misura con due gradi di
libertà su un confronto interno (punteggi di rosa), uno è bloccato da un
difetto di Livello 3 (decisioni d'asta). E l'unico verdetto completo che
esiste è contro il cubo.

---

## 5. Che cosa manca per «collegato»

Meno di quanto sembri, e non è una buona notizia: manca tutto, ma il pezzo
mancante è piccolo e ben definito.

RIPRODOTTO: nessun pack porta `theta`; i quattro moduli del percorso d'asta non
nominano né `cubo` né `tabellino`.

Perché `theta` possa entrare in un pack servirebbero tre cose che oggi non
esistono, tutte OSSERVATE NEL CODICE:

1. un posto dove metterlo. `SeasonPack` ha dieci attributi fissi e nessuna
   versione, nessun manifesto, nessuna validazione: `STATO_LIVELLI_20260909.md`
   lo dice già («i consumatori usano `getattr` con valori di riserva»);
2. un consumatore che lo legga. Oggi `theta` viene applicato scrivendo dentro
   `m_part.logit_base_convocato` per la durata di una simulazione
   (`scripts/l2_pilota_c2.py:313-330`, con ripristino in `finally`). È una
   modifica temporanea di un oggetto in memoria, non un parametro che un
   artefatto porta con sé;
3. una ragione. `theta` è definito su 30 giocatori di una squadra di una
   stagione. Non c'è una regola scritta che dica che cosa significhi applicarlo
   a un'altra stagione, e la verifica esistente chiama quel caso
   «TRASFERIMENTO» e lo distingue dalla verifica dello stesso candidato
   (`scripts/l2_verifica_c2.py:126-128`).

IPOTESI, dichiarata come tale: collegare `theta` prima che la colonna
«validato» sia risolta significherebbe portare in produzione un parametro
calibrato verso un bersaglio contaminato. Non lo raccomando, e non è la mia
decisione.

---

## 6. La prova decisiva del §3ter: eseguibile oggi?

**No.** Tre ostacoli, due di codice e uno di tempo.

### Ostacolo 1 — il pilota non sa ricevere un altro bersaglio

OSSERVATO NEL CODICE. `costruisci_ingressi`
(`scripts/l2_pilota_c2.py:227`) costruisce il bersaglio così, alla riga 248:

```python
bers = presenze.costruisci(proc / f"b_predictions_{stagione}.json",
                           giocatori, ruolo,
                           storia_voti=Ppre[["master_id", "stato_voto"]])
```

Il percorso è calcolato dalla stagione, non è un parametro. Gli argomenti di
riga di comando (`scripts/l2_pilota_c2.py:363-386`) sono sedici e nessuno
riguarda il bersaglio: `stagione`, `--cutoff`, `--sims`, `--sims-monitoraggio`,
`--sims-verifica`, `--semi-ottimizzazione`, `--semi-monitoraggio`,
`--semi-verifica`, `--seme`, `--iterazioni`, `--lam`, `--modo-bersaglio`,
`--peso-bersaglio`, `--squadre`, `--n-squadre`, `--istante`. Il passo 1 di
`RIPRESA.md` §4 — «dare a `partecipazione.stima` il bersaglio per origine
invece di `b_predictions`» — **non ha oggi un punto di aggancio**.

### Ostacolo 2 — i due formati non combaciano

OSSERVATO NEL CODICE. `scripts/l1_presenze_per_origine.py:257-262` scrive un
CSV con le colonne `master_id`, `presenze_residue_attese`, `scarto_fra_semi`,
`presenze_gia_fatte`, `orizzonte_giornate`.
`presenze.costruisci` (`src/fantabot/tabellino/presenze.py:101`) vuole un JSON
`id → {"pres": …}` dove `pres` è la **somma** di presenze già fatte e previste,
più `giornate_residue` e `presenze_gia_fatte` passati a parte. Serve un
adattatore di una decina di righe, e va provato, perché è esattamente il punto
in cui in passato si è sbagliata l'unità di misura (la docstring di `presenze`
lo dichiara alle righe 108-115).

### Ostacolo 3 — il tempo

RIPRODOTTO dal log del candidato (`data/l2/log_pilota_c2_v2.txt`): «modelli in
34.2 s», poi «200 iterazioni, 406 valutazioni di ottimizzazione + 32 di
monitoraggio, 5483.6 s». Cioè circa 12,5 s per valutazione a `sims 6`, e
**91 minuti e mezzo per la sola ottimizzazione**. La fase di verifica appaiata
(16 simulazioni, 5 semi, due bracci) costa, alla stessa velocità, altri 5-6
minuti. Totale stimato **95-105 minuti**, contro un limite di quindici.

Va aggiunto che il pilota condivide la CPU con altri agenti stanotte, quindi la
stima è ottimistica.

### La prova, descritta per intero

**Domanda.** Con un bersaglio costruito solo con feature disponibili alla data
di decisione, il cubo si avvicina alle presenze realizzate più di quanto faccia
il bersaglio da solo?

**Comando** (l'opzione `--bersaglio` non esiste ancora: è il passo 1 di §7):

```
PYTHONIOENCODING=utf-8 PYTHONPATH=src OMP_NUM_THREADS=2 \
python scripts/l2_pilota_c2.py 2024-25 --cutoff 2024-08-17 \
  --bersaglio data/l1/presenze/2024-25__origine20240816__a6a7eb010299/presenze_per_origine.csv \
  --iterazioni 200 --sims 6 --sims-verifica 16 --semi-verifica 5
```

**Durata stimata**: 95-105 minuti, di cui 92 di ottimizzazione. Da eseguire
quando la CPU è libera, e non prima dell'asta.

**Dati necessari**, tutti già su disco e verificati stanotte:
`data/l1/presenze/2024-25__origine20240816__a6a7eb010299/presenze_per_origine.csv`
(esiste, insieme a una seconda origine `20241001`);
`data/processed/players_2024-25.parquet` (impronta `700a9b471e21780a`,
invariata); `data/processed/l2_partite.parquet` (`f725c558b6da934b`,
invariata); i panel `data/processed/l2_panel_*.parquet` dell'8 settembre.
Nessun dato nuovo da scaricare. Nota: l'origine disponibile è il 16 agosto
2024, il cutoff del pilota è il 17: la differenza di un giorno va dichiarata
nell'artefatto oppure risolta rigenerando la previsione all'origine
`2024-08-17`.

**Criterio di accettazione, scritto adesso, prima di avere qualsiasi numero
nuovo.**

1. Misura primaria: errore assoluto medio per giocatore fra presenze simulate e
   **presenze realizzate**, sugli stessi cinque semi di verifica per entrambi i
   bracci, con differenze appaiate seme per seme.
2. Bracci: `bersaglio onesto da solo` contro `cubo alimentato dal bersaglio
   onesto`.
3. Il cubo è dichiarato **utile sulle marginali** solo se la differenza
   appaiata media è a suo favore e `|d| / es(d) ≥ 2,776`, cioè la t di Student
   al 95 % con quattro gradi di libertà (cinque semi). Con tre semi la soglia
   sarebbe 4,303, come l'artefatto della verifica già dichiara.
4. Se l'intervallo attraversa lo zero: **inconcludente**, e resta inconcludente.
   Margine di non inferiorità zero, come impone `reports/CRITERI_L2_L3.md` §1.2.
5. Se il cubo peggiora con intervallo che esclude lo zero: si scrive che
   peggiora, e non si compensa con un'altra misura.
6. Nessuna soglia si sposta dopo aver visto i numeri; nessun braccio si
   aggiunge dopo; nessun seme si scarta.
7. I quattro numeri già pubblicati — 0,1453 (bersaglio contaminato), 0,2136
   (bersaglio onesto), 0,1749 (cubo con bersaglio contaminato), 0,2530 (cubo
   senza bersaglio) — sono riferimenti di contesto e **non** soglie. In
   particolare non è ammesso dichiarare un successo perché il numero nuovo
   cade sotto 0,2530.

**Che cosa questa prova non decide.** Non decide se il cubo serva: decide se
la calibrazione delle marginali abbia senso a ingressi puliti. Le dipendenze,
la durata delle assenze e le decisioni d'asta restano fuori, e sono i luoghi
dove il report del 9/9 dice che il cubo dovrebbe guadagnare.

---

## 7. Passi minimi

Ognuno con file, prova, accettazione e ripristino. Nessuno va eseguito prima
dell'asta di stasera.

**1. Parametro del bersaglio nel pilota.**
File: `scripts/l2_pilota_c2.py` (firma di `costruisci_ingressi` alla riga 227,
uso alla 248, argomenti alle 363-386).
Prova: due casi nuovi in `tests/test_l2_ingressi_c2.py` — senza l'opzione, il
dizionario restituito da `IngressiC2.vettori()` è identico a quello di oggi
elemento per elemento; con l'opzione, `bersaglio_grezzo` cambia e `universo`
no.
Accettazione: le 361 prove del cubo continuano a passare, più i due casi nuovi.
Ripristino: copia del file in `data/l2/ripristino_<data>/` prima di toccarlo,
come il progetto fa già (`data/l2/ripristino_20260909c/scripts__l2_pilota_c2.py`).

**2. Adattatore dal CSV per origine al bersaglio.**
File: nuovo, oppure una funzione in `scripts/l2_pilota_c2.py`.
Prova: un CSV finto con `presenze_gia_fatte` e `orizzonte_giornate` noti
produce le probabilità attese a mano; un identificativo assente finisce nel
prior di ruolo e non a zero.
Accettazione: prove nuove verdi, e somma dichiarata uguale a somma ottenuta,
come già impone `bersaglio_verso_somma`.
Ripristino: file nuovo, si cancella.

**3. Rieseguire il controfattuale sul produttore corrente.**
File: nessuna modifica.
Comando: `PYTHONPATH=src python scripts/l2_controfattuale_c2.py 2024-25`.
Accettazione, scritta prima: `"passato": true`,
`"dipendenze_senza_potenza": {}`, e `impronta_produttore` uguale all'impronta
del file su disco al momento della corsa.
Ripristino: lo script scrive solo sotto `data/l2/prove/`, che è una radice
separata da quella dei risultati.

**4. Aggiungere l'impronta dello script al controllo di coincidenza.**
File: `scripts/l2_verifica_c2.py:64-128`.
Prova: un piano con `impronta_script` diversa deve comparire nel risultato come
voce dichiarata; il caso del candidato `89314011c6c4` con il codice attuale
deve segnalare `6803831dafa51bef → e688e36901b3fa86` invece di tacerla.
Accettazione: la verifica non può più dire «stesso candidato» quando il codice
che ha prodotto il candidato non è quello in esecuzione. Resta da decidere se
la differenza debba far fallire o solo comparire: il codice già distingue i due
regimi per i moduli, e la stessa scelta va scritta prima, non dopo.
Ripristino: come sopra.

**5. Registrare l'impronta del bersaglio nel rapporto del cubo, e rigenerare
il cubo 2026-27.**
File: `scripts/l2_genera_cubo.py:91` e la sezione che scrive
`cubo_rapporto_*.json`.
Costo misurato dal log dell'ultima generazione: 105,0 s di stima più 44,6 s di
generazione, per 40 scenari.
Accettazione: le quattro impronte di `contratti_panel/2026-27/fonti`
coincidono con i file su disco al momento della corsa, e il rapporto porta
l'impronta di `b_predictions_2026-27.json`.
Ripristino: archiviare il cubo vecchio in `data/l2/archivio/` prima di
sovrascrivere. **Da non fare prima dell'asta**: rigenerare non è autorizzato in
questo ciclo, e il cubo non è sul percorso operativo, quindi non c'è urgenza.

**6. La prova decisiva di §6**, dopo i passi 1, 2 e 3.

**7. Solo se la prova decisiva non è inconcludente**: portare la misura dove il
cubo lavora — distribuzione della durata delle assenze simulate contro
osservata, con criterio scritto prima. Oggi quella misura non esiste in nessun
file del progetto.

---

## 8. Che cosa ho eseguito stanotte, e che cosa no

Eseguito: una sola suite pytest,
`tests/test_l2_*.py`, 360 passate, 1 saltata, 125,72 s. Letture di file,
calcoli di impronte in memoria, ispezione dei tre pack in sola lettura.

Non eseguito, per mandato o per costo: `scripts/l2_controfattuale_c2.py` e
`scripts/l2_pilota_c2.py` (scriverebbero sotto `data/l2/prove/`, e il compito
vieta di creare file del progetto); `scripts/l2_banco_confronto.py` (due corse
da `--sims 30 --semi 8`); `scripts/l2_genera_cubo.py`; la suite intera, di cui
riporto il numero 562 come AFFERMATO in `RIPRESA.md` e non riprodotto.

Effetto collaterale inevitabile della suite: i file `.pyc` sotto
`tests/__pycache__` e `src/fantabot/**/__pycache__` sono stati riscritti da
Python. Nessun file sorgente, dato o artefatto è stato toccato.
